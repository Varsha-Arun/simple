resources:
  - name: releaseVersion
    type: PropertyBag
    configuration:
      value: test
      pin:
        versionId: 115

pipelines:
  - name: P_Bash_001
    configuration:
      environmentVariables:
        readOnly:
          resource_set: 2
          resource_check: 1
    steps:
      - name: S_Bash_001_1
        type: Bash
        configuration:
          outputResources:
            - name: releaseVersion
        execution:
          onExecute:
            - write_output releaseVersion value=$run_number
            
  - name: P_Bash_002
    steps:
      - name: P_Bash_002_1
        type: Bash
        configuration:
          inputResources:
            - name: releaseVersion
        execution:
          onExecute:
            - echo $res_releaseVersion_value
#             - if [ "$res_releaseVersion" != "$resource_check" ]; then exit 1; fi
