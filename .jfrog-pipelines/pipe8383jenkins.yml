pipelines:
  - name: P_WF_pipelines
    configuration:
      integrations:
        - name: auto_ssh
      environmentVariables:
        readOnly:
          IP_ADDRESS: 
            allowCustom: true
            default: "100.10.158.24"
          User: "varshaa"
    steps:
      - name: P_WF_step1
        type: Bash
        configuration:
          inputResources:
            - name: r_WF_gr
          outputResources:
            - name: pb_branchSnapshot_version
        execution:
          onExecute:
            - echo $IP_ADDRESS
            - pushd $res_r_WF_gr_resourcePath
            - local pem_file_path=/tmp/pem_key
            - echo -e ${int_auto_ssh_privateKey} > pem_file_path
            - chmod --help
            - chmod -cR 600 $pem_file_path
            - scp -i $pem_file_path -o StrictHostKeyChecking=no -o NumberOfPasswordPrompts=0 -P 22 installJenkins.sh $IP_ADDRESS:/home/$User
#             - ssh -i pem_file_path $IP_ADDRESS "cd /home/$User; ./installJenkins.sh"
#             - api_token=090909090909090900900
#             - write_output pb_branchSnapshot_version api_token=$run_number
      
resources:
  - name: r_WF_gr
    type: GitRepo
    configuration:
      gitProvider: varsha_github
      path: Varsha-Arun/simple
      branches:
        include: master
        
  - name: pb_branchSnapshot_version
    type: PropertyBag
    configuration:
      api_token: ""
      


