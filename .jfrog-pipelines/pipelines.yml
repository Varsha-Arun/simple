pipelines:
  - name: pipeline1matrix
    steps:
      - name: testmatrix1
        type: Matrix
        stepMode: Bash
        stepletMultipliers:
          environmentVariables: 
            - env1: one 
            - env2: two
        execution:
          onExecute:
            - echo lol
